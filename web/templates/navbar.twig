<div class="row">
   <div class="col-md-12" ><img width="100%" src="data/{{ page_id }}/bg.jpg"/></div>
</div>

{% import 'bs3-forms' as forms %}

<nav role="navigation" class="navbar navbar-default">
    <div class="navbar-header">
                <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
<!--                <a href="page/{{ page_id }}/" class="navbar-brand">{{nav_brand}}</a> -->
    </div>

    <div id="navbar" class="collapse navbar-collapse">

  		{% include 'menu' with menu %}

            <ul class="nav navbar-nav navbar-right">
                {% if username  %}
                        <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                        <span class="glyphicon glyphicon-user"></span>
                                        <strong>{{ username }}</strong>
                    <span class="glyphicon glyphicon-chevron-down"></span>
                        </a>
                        <ul class="dropdown-menu" role="menu">
	                         <li><a  href="#" id="logout">Logout</a></li>
                             <li role="separator" class="divider"></li>
                             <li><a href="pages/edit/">Edit Pages</a></li>
                             <li><a href="users/edit/">Edit Users</a></li>
                             <li><a href="routes/edit/">Edit Routes</a></li>
                        </ul>
                </ul>
                        </li>
		{% else %}
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b>Login</b> <span class="caret"></span></a>
			<ul id="login-dp" class="dropdown-menu">
				<li>
					 <div class="row" id="login-form">
							<div class="col-md-12">
								{{ forms.start() }}
								{{ forms.input('username', type='text', placeholder="Username", required = true) }}
								<div class="form-group">
									 <label class="sr-only" for="password">Password</label>
									 <input type="password" class="form-control" id="password" name="password" placeholder="Password" required>
                                	 <div class="help-block text-right"><a href="user/password">Forget the password ?</a></div>
								</div>
								<div class="form-group">
									 <button type="submit" class="btn btn-primary btn-block">Sign in</button>
								</div>
								<div class="checkbox">
									 <label> <input type="checkbox" name="remember-me"> Remember me </label>
								</div>
								{{ forms.end() }}
							</div>
							<div class="bottom text-center">
								New here ? <a href="user/register"><b>Register</b></a>
							</div>
					 </div>
					
				</li>
			</ul>
		</li>		
		{% endif %}
                  </ul>
        </div>
</nav>

