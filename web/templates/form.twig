<form {% if action is defined %}action="{{action}}"{% endif %} {% if method is defined %}method="{{method}}"{% endif %} {% if enctype is defined %}enctype="{{enctype}}"{% endif %}>
	{% with data %}
		{% include 'form-base' %}
		<button type="submit" class="btn btn-default" name="{{button.name}}">{{button.title}}</button>  
	{% endwith %}
</form> 

